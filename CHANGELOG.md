# 更新日志

## V7.7（2026-09-01）

### 数据安全（脱敏）
- **移除代码中所有业主单位名称**：DEFAULT_DATA 演示数据的项目名称置空、detectCompany 过滤正则、company 二次过滤、规则弹窗文案
- 制造厂家识别改用**纯标签驱动**过滤：通过"客户名称/Customer/项目名称/Project Name/采购单位"等通用字段标签拦截甲方信息，**不依赖写死具体业主名**，避免敏感信息进入代码
- 一览表模板 `templates/材料质量证明文件一览表V1.docx` 清空工程名称与领用/采购单位中的业主信息（保留制造厂家）
- 工具**从不读取、不保存、不输出**业主单位/项目名称字段（解析结果仅含产品/材质/规格/数量/炉批号/编号/厂家等物资字段）

### 分类定义修正（管材 / 管件）
- **问题**：`classifyProduct` 中管材识别含 `/钢管|无缝|焊管|流体输送/`，"无缝"过于宽泛，导致"无缝弯头90°""无缝三通"被误判为**管材**
- **业务定义**（用户确认）：
  - **管材** = 钢管类别（钢管/焊管/流体输送）
  - **管件** = 三通、弯头、大小头、管帽、异径管等配件
- **修复**：调整识别顺序——**管件优先于管材**；从管材识别词中**移除"无缝"**
- 影响：序号29 第 2-5 条由「管材」更正为「管件」

### 序号29（亮亮）解析修复
- **现象**：11 页 JSON 只解析出 10 条，且第 6-10 条规格型号/炉号/批号为空
- **根因**：`parseHengtongCert` 检测条件用 `table.slice(0, 5).join('|')` 拼接**前 5 行**查找"供货状态"关键词
  - 序号26 的表格"供货状态"恰好在前 5 行内 → 通过
  - 序号29 部分页面首行多了"收货单位"，把"供货状态"推到 row5 之外 → 检测失败 → 该页落入通用解析器 → 漏 1 条 + 字段错位
- **修复**：检测拼接改为全表扫描 `table.map(r => r.join('|')).join('|')`，不再依赖行号假设
- 修复后：11 条质保书全部解析，规格型号/炉号/批号/证书出厂编号全部正确

### 事故复盘：V7.6 的"稳定"是假象
- 用 V7.6（未脱敏原版）直接跑序号29 复现**完全相同**的错误 → 证明这是 **V7.6 就存在的潜伏 bug**，与 V7.7 脱敏改动无关
- 脱敏只改了 `detectCompany` 的过滤正则；出错的 `parseHengtongCert` / `classifyProduct` 在 V7.7 中一行未动（除本次修复本身）
- **结论**："稳定"只对已测数据成立，新数据变体会引爆潜伏 bug → 见 `docs/开发规范.md` 三条铁律

### 测试结果
- 16 通过 / 9 偏差（既有OCR数据问题）/ 0 缺失 / 0 错误 / 总计 25
- 序号29：11 条全部正确（条数、规格型号、炉号、批号、证书编号、分类）

### 网页版部署（给链接就能用）
- 工具是**纯前端单文件**（零外部依赖、零后端），可部署为网页版，解决「发 HTML 文件 + 教别人用浏览器打开 + 各浏览器表现不一」的痛点
- 经 HTTP 提供后 Chrome / Edge / Firefox / Safari 行为一致；导入导出均在浏览器本地完成，**文件不上传服务器**
- 新增部署包：`deploy/index.html`（V7.7 副本，加浏览器一致性页脚提示）+ `deploy/nginx-qc-quicklog.conf`（nginx 静态托管 + HTTP Basic 口令模板）
- 部署步骤见 `docs/部署指南.md`（Linux + nginx，含 HTTPS / 升级 / 排错）

---

## V7.8（2026-09-03）

### 法兰解析器泛化 + 开会后 2 个材料修复
- **问题**：开会后的 2 个序号（承插焊法兰、锻件法兰合格证）解析错误/丢条
  - 序号30（无锡市华尔泰 承插焊法兰）：落入 `parsePipe` 被误判为「管材」，产品名称变成默认兜底"输送流体用无缝钢管"
  - 序号31（浙江大齐 锻件产品合格证，10 行法兰清单）：0 条解析（无任何解析器匹配"序号/名称/标准/规格/数量"清单格式）
  - 厂家识别：序号30 因页面含"项目名称"被 `detectCompany` 整体清空；序号31 误把"订货单位"后的甲方名当厂家

### 根因
- `parseWeldFlange` 仅匹配"对焊法兰"字面 → 承插焊法兰/法兰盖/带颈法兰等全部漏过
- `parsePipe` 用"牌号Steel Grade"作为管材信号过于宽泛：法兰质保书的原材料块也有该表头 → 误路由
- 锻件法兰合格证是**多行产品清单**（同一证书多个法兰），原无对应解析分支
- `kvLookup` 只向右找同行的下一个单元格：原材料块是"跨行合并单元格"（标签行 + 下一行同列才是值），导致炉号取到"锻件级别Forging Grade"标签
- 去重 key 不含"数量"：同规格不同数量的法兰行会合并丢失

### 修复（三处同步：规则Excel + classifyProduct + 测试断言）
- **`parseWeldFlange` 泛化**：兼容两类法兰质保书
  - A. 产品描述单产品表（表头含 名称/规格/牌号/数量）→ 承插焊法兰、对焊法兰等
  - B. 产品清单多行表（表头含 序号/名称/标准/规格/数量）→ 锻件法兰合格证，逐行产出（可能返回多条）
  - 检测：找到含"名称"且含"规格/标准"且含"数量"的表头行，且其后存在名称含"法兰/FLANGE"的数据行
- **`kvLookup` 增强**：① 跳过"看起来像字段标签"的单元格（锻件级别/热处理炉号等），避免把下一个标签当值；② 合并单元格场景回退到**下一行同列**取值（原材料跨行标签）
- **`detectCompany` 加固**：新增厂家映射（华尔泰 / 大齐）；过滤标签增加"订货单位"（甲方，非制造厂家）
- **去重 key 增加"数量"**：同规格不同数量的行不再合并（序号31 的 10 行去重后保留 9 条唯一，另 1 条为真正重复的行项）

### 测试结果
- 18 通过 / 9 偏差（既有OCR数据问题，与 V7.7 一致）/ 0 缺失 / 0 错误 / 总计 27
- 新增夹具：`tests/fixtures/序号30-华尔泰承插焊法兰.json`、`序号31-大齐锻件法兰.json`（对应断言见 `test_v74_json.js`）
- 规则同步：`rules/8.28-V7.4规则更新.xlsx` 追加 序号30 / 序号31 行（物资分类均为「法兰」）

> 说明：V7.8 文件夹命名含"3个材料"，本次仅收到 2 个 JSON（序号30、序号31）；若有第 3 个材料，按同样流程补夹具 + 断言即可。

### 序号32（开会后第3份，12页混合质保书）解析修复
- **现象**：12 页混合质保书（多种物资、多家厂家合订）只解析出前 3 条，其余页全部漏解析
- **逐页诊断根因**（11 份证书，其中页5 为恒隆某证书的纯化学成分续页，产品主表不在 PDF 中，合理跳过）：
  - 页2 恒隆：被 `[名称,材质,厂家]` 去重吞掉（同厂家同材质不同规格）→ **管材去重 key 加入规格型号**
  - 页4 石油裂化用无缝钢管：产品名称正则不匹配 + colspan 重复单元格 → `parsePipe` 放宽正则并跳过重复单元格；材质格式2 支持 `20+Zn`
  - 页7 金洲镀锌管：多行物料清单无解析分支 → 新增 `parsePipeJinzhou`（物料名称/规格/钢号/重量(吨)/产品批号，逐行产出）
  - 页8-10 正丰槽钢/角钢：无型钢解析分支 → 新增 `parseSteelSection`（兼容布局A「型号+炉批号+吨数」表头、布局B「炉批号+重量」第二块、布局C「名称/炉批号/型号/牌号」数据行前置捕获）
  - 页11/12 宝旭卷板/武钢钢带：无板材解析分支 → 新增 `parseSteelPlate`（表头要求钢卷号+炉号+**专用**重量列并排除「规格及重量」混合单元格；重量从专用列起扫描，被跳过的小整数作为件数，kg→吨；KV「厚度×宽度×长度=」取规格）
- **新增第8分类「型钢/板材」**：槽钢/角钢/工字钢/圆钢/扁钢/钢板/卷板/开平板/花纹板/钢带/钢坯等结构材料（此前落入默认「管件」）；`inferUnit` 默认单位「吨」
- **`detectCompany` 新增厂家映射**：唐山正丰钢铁 / 浙江金洲管道 / 武汉钢铁
- **`extractCertNo` 新增两模式**：`证明书号：`、`质保书号 MILL SHEET NO.`
- **厂家回退加固**：管材分发与三个新解析器分发链均加甲方标签保护（客户名称/收货单位/采购单位/项目名称等标签命中时不继承当前厂家），修复页4/页11 厂家误继承「东群」

### 批量生成证件自编号优化（消除歧义）
- 默认前缀 `ZM-` → `260803-`；起始序号默认 **1001**；序号位数（补零）默认 **4**
- 新增**实时预览**：打开弹窗即显示「将为「分类」中 N 条未编号记录生成：260803-1001 起逐条自动累加，末尾编号 260803-1020；已填过编号的记录自动跳过」——不再需要猜测"结束序号"
- 语义明确化：适用分类标注为「先按分类筛选，再对筛选结果顺序编号」——选"全部"时对所有未编号记录从起始序号连续累加；勾选某类别时只对该类别的未编号记录编号

### 测试结果（含序号32）
- **39 通过 / 9 偏差（与基线 9 项完全一致）/ 0 缺失 / 0 错误 / 总计 48**
- 序号32-51（记录级编号，12页 → 20 条记录 / 11 份证书）全绿；序号7 由偏差恢复通过（去重 key 加规格后不再吞掉真实第 5 份证书 00368629，按铁律补 `'5-8附加'` 断言）
- 新增夹具：`tests/fixtures/序号32-开会后第3份混合12页.json`
- 规则同步：`rules/8.28-V7.4规则更新.xlsx` 追加 序号32 行

> 注意：①「型钢/板材」为新增第 8 分类，需业务确认归类口径；②测试断言 32-51 为**记录级编号**（3.json 的 11 份证书拆 20 条记录），与正式序号分配可能不一致，如用户有自己的序号 32 分配需告知调整；③页5（恒隆纯化学成分续页）无产品主表、页4（强英）厂家留空，需手工补填。

### V7.8 收尾修订（2026-09-03 晚，用户确认后落地）
- **升版 V7.7 → V7.8**：页面标题 / 徽标 / 使用规则标题同步
- **型钢/板材 独立统计标签（用户确认第8分类）**：修复 `renderImportStats` 的 `catOrder` 列表仍写旧名"型材"导致统计卡片与分类下拉不显示——改为「型钢/板材」，分类统计卡片、顶部筛选、批量自编号弹窗的分类下拉均正常显示并计数
- **管材材质带出标准编号**：原规则"材质去除标准号"修订为"**用逗号带出**"——格式3（鲁宝/衡阳）从标准串提取材质时保留标准号（如 `20, GB/T 8163-2018`）；新增 return 前通用后处理：材质中不含标准号时从表格文本补找 `GB/T xxx` 追加（`20+Zn` 等镀锌牌号同样适用）
- **管材单位统一为吨**：`parsePipe` 三处 `KG` 判定改为 **kg÷1000 折算、保留全部小数不截断**（如 126→0.126、377→0.377、20126→20.126、13→0.013）；后续如有 KG/米等单位输出需求，再按解析器保留原始单位并加单位输出开关
- 回归：**39 通过 / 9 偏差（与基线一致）/ 0 缺失 / 0 错误 / 48 总计**；序号32/33/34/36 数量断言按折算值同步更新（126→0.126 等，并补"单位: 吨"断言）

### ⚠️ 仓库事故记录（.git 对象库损坏与恢复）
- 本轮会话中 `.git/objects` 全部松散对象与 `.git/refs` 目录被外部清空（原因不明，疑与磁盘同步/清理工具相关；此前已有"refs 写入不持久"前科），本地 3 个未推送提交（43bd12d / da7e1b1 / e02b871）对象丢失
- **工作区文件完好无损**，内容即最新状态；远端 GitHub `feat-v7.7-data-privacy` 分支顶点 1e7b053（丢失提交之前）
- 恢复方式：`git fetch`（直连 GitHub 大 pack 传输易被重置，`-c protocol.version=0` 已验证可完成单分支 fetch），fetch 成功后将本地分支 ref 指到远端顶点 1e7b053，再把当前工作区提交为新提交（累计内容 = 丢失 3 提交 + 本轮修订）

---

## V7.6（2026-08-30）

### 新增序号（2个）
- **序号26**：有缝弯头90°（河北恒通管件集团有限公司，JL/ZJ-25 KV格式）
  - 证书出厂编号 20260625292 / 材质 Q245R GB713 / 规格 DN1000×12 R=1.5D / 炉号 231669400 / 批号 23S-043902 / 3 件
  - 新增 `parseHengtongCert` 解析器 + `kvLookup` 工具函数
- **序号27**：闸阀/截止阀 6 条（上海美科阀门有限公司，多行清单表）
  - 证书出厂编号 261394-PC03 / 材质 WCB / 规格 Z41H-25 DN200 等 / 数量 17、9、2、2、1、2 台
  - **新增"工单号"字段**：从表格"工单号No."列提取（618800030697），底表不显示，供检测委托单导出使用
  - 新增 `parseMeikeValve` 多行解析器 + 厂家映射"上海美科阀门有限公司"

> 序号28（对焊法兰，常武欣摄）用于检测委托单导出测试，已补全熔炼号 25-211823 / 锻件批号 260307-01。

### 导出升级
- **一览表改为 Word 导出**：对齐 `templates/材料质量证明文件一览表V1.docx`（SH/T 3503—J132 横向模板）
  - 14 列模板列宽、表头宋体/数据楷体、标题 16pt 加粗、全边框
  - 超 22 条自动分页带**（续）**；证件自编号为空填 **"/"**
- **新增"导出检测委托单"**：替换原"导出JSON"按钮，对齐 SH/T 3543—G128 横向模板（23 列）
  - 字段映射：检件名称=产品名称、质量证明文件编号=证书出厂编号、炉/批号=炉号(无则批号)、
    检件规格=规格型号、检件材质=材质、数量=数量、工单/网络号=工单号
  - 检件编号/热处理状态/制造验收标准/委托单位/委托人/日期 → 留空供手工填写后打印
- **工程名称可编辑**：工具栏输入框，默认空白，导出时为空会弹窗提示输入（取消或留空则中止导出）
- 底表 CSV：证件自编号为空导出填 "/"

### 导入升级
- **批量导入**：JSON/MD 入口支持一次多选多个文件，逐个解析后汇总，日志显示每个文件的成功/失败/条数

---

## V7.5（2026-08-28）

### 功能新增
- **核心字段可手动编辑**：明细表格中「产品名称/规格型号/材质/数量/单位/炉号/批号/制造厂家/证书出厂编号」全部变为可编辑输入框（原先只有证件自编号/复验报告编号可编辑），解析偏差可直接在表格内修改
- **单行删除按钮**：每行最前面新增 🗑️ 删除按钮，点击确认后删除整行（解决多读一行、批量清空误删的问题）

### 交互改进
- `updateField` 支持所有字段的 filled 样式反馈（非空字段绿色高亮）
- `deleteRow` 删除前弹出确认框（显示序号+产品名称），删除后自动刷新并提示剩余条数

### 测试结果
- 16 通过 / 9 偏差（既有OCR数据问题）/ 0 缺失 / 0 错误 / 总计 25（解析逻辑无回归）

---

## V7.4（2026-08-28）

### 物资分类扩展：新增「支吊架」（第7类）
- 原来6类：阀门、对焊法兰、管材、紧固件、垫片、管件
- V7.4新增：**支吊架**（吊架/支吊架/碟簧吊架/弹簧支架），独立分类，**不属于管件**
- `classifyProduct` / `inferUnit` 增加支吊架识别，单位默认「套」
- 序号25（泰克装箱清单 可变碟簧吊架）分类由「管件」修正为「支吊架」

### 新增
- 新增序号23测试用例：无缝管（青山钢管有限公司）
  - 证书出厂编号 011001733602（CERTIFICATE NO. 证书编号：01 100 1733602）/ 材质 06Cr19Ni10 / 规格 26.7\*2.87\*5000-6800 / 炉号 YX2505-2263 / 批号 4T260606-18 / 数量 303 KG
- 新增序号24测试用例：仪表卡套无缝管（浙江方顿仪表阀门有限公司）
  - 证书出厂编号 20260110203 / 材质 304 / 规格 ∅12×1.0×6000 / 炉号+批号 YX2505-1554 / 数量 300 m
- 新增序号25测试用例：可变碟簧吊架（扬州市泰克管道机械有限公司 装箱清单）
  - 规格 BDC20E 1568N 1815N 15↑ GB/T 28707 组合件 / 数量 1 套（同表"配套件"行解析为多余记录，需人工确认）
- 规则文件：`rules/8.28-V7.4规则更新.xlsx`（含序号1-25全部规则，7类分类）

### 解析器改进
- 新增厂家映射：青山钢管有限公司、浙江方顿仪表阀门有限公司、扬州市泰克管道机械有限公司
- 新增 `parsePipeQingshan`：青山钢管英文表头无缝管解析（CONTRACT NO/PRODUCTION LOT NO/HEAT NO/SIZE/WEIGHT 格式）
- 新增 `parsePipeFangdun`：方顿仪表卡套无缝管解析（钢管批号 Lot No./材质 Material/规格 Size(mm) 格式），炉号从化学成分表"原材料炉号Heat No."列补充
- 新增 `parseBoxListTaike`：泰克装箱清单解析（序号/ERP订单号/物料编码/规格型号/数量/单位/备注/装箱 格式）
- `extractCertNo` 支持空格编号去空格（"01 100 1733602" → "011001733602"），修复邮编被误判为编号的问题
- JSON 表格解析纳入 table_caption 文本（解决装箱清单厂家识别）

### 测试结果
- 16 通过（含序号23/24/25全部通过）/ 9 偏差（既有OCR数据问题）/ 0 缺失 / 0 错误 / 总计 25

---

## V7.3（2026-08-26）

### 新增
- 新增序号22测试用例：对焊法兰（常州市常武欣摄石化配件有限公司）
  - 证书出厂编号 26-06-3092 / 材质 A105 / 规格 WN350-150LB/RF SCH30 / 数量 8
- 规则文件更新：`rules/8.26-V7.3规则更新.xlsx`（含序号1-22全部规则，替代旧V6.0规则文件）

### 解析器改进
- DetailList 表头 nameCol 匹配支持"产品名称"列（原仅匹配"名称"）
  - 正则：`/名称/.test(h) && !/编码|材料|批号/`
- DetailList 规格处理：规格含 ≥2 个关键标记（CL/DN/SCH/RF）时保持原样
  - 修复法兰规格 `WN350-150LB/RF SCH30` 被拆分为 `SCH30 RF` 的问题

### 测试结果
- 13 通过 / 9 偏差（OCR数据问题，暂不处理）/ 0 缺失 / 总计 22

---

## V7.2（2026-07-31）

### 解析器改进
- `parseTableHtml` 支持 rowspan/colspan
- `discarded_blocks` 页眉页脚提取（编号/厂家常在此处）
- `extractCertNo` 支持 6 位日期格式（如 ZH-QC-250327-15）
- 编号/厂家回退链：页面文本 → 表格文本 → discarded_blocks
- DetailList OCR 变体支持（"Derailed List"）
- 管材多页去重、紧固件/垫片去重
- 阀门"物料描述"跨行匹配、"阀体"下一行同列匹配
- 管件 KV 格式回退、多行数据 `_allRecords`
- 编号噪音过滤（排除 CERTIFICATE/REPORT/质量证明书等）

### 测试结果
- 12 通过 / 9 偏差 / 0 缺失 / 总计 21

---

## V7.0（2026-07-30）

- JSON 导入替代 HTML，累积导入 + 清空按钮
- 使用规则模态框，底表数据视图
- 6 大分类解析器：阀门/对焊法兰/管材/紧固件/垫片/管件

## V6.0（2026-07-28）

- 新增 6 类物资解析器
- 新增厂家关键词映射
